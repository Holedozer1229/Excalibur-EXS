[package]
name = "fair_lattice"
version = "0.1.0"
description = "Fair lattice bonding curve + referral attributes on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "fair_lattice"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
anchor-spl = { version = "0.31.1", features = ["associated_token"] }
