# Excalibur $EXS - Vercel Ignore File
# Prevent Vercel from trying to build or deploy certain directories

# Development/alternative Next.js UI (not used in production)
/web/forge-ui

# Build artifacts and dependencies (already in .gitignore, but explicit here)
node_modules
.next
.cache

# Go backend (not deployed to Vercel)
/cmd
/pkg
*.go

# Python scripts (not deployed to Vercel)
*.py

# Build and test files
/test.sh
/scripts

# Docker files (not needed for Vercel deployment)
/docker
/docker-compose.yml

# Mobile app (not needed for web deployment)
/mobile-app

# Ledger and backend data
/ledger
